# How to make beautiful UIs with AI

## 影片基本資訊
- **影片 ID**: PlQPSkIUdIk
- **頻道**: (Creator Channel)
- **觀看次數**: 50K
- **連結**: https://www.youtube.com/watch?v=PlQPSkIUdIk

## 學習目標
- 了解 v0 背後使用的技術：shadcn/ui
- 學會在 Cursor AI 中直接使用 shadcn/ui 元件
- 掌握 React + Tailwind + shadcn 的專案設置
- 學會客製化主題和深色模式

## 核心觀念

### v0 的秘密：shadcn/ui
- **v0 之所以好看，不是因為 v0 本身**
- **真正的魔法來自 shadcn/ui**
- shadcn/ui 是 React 的設計系統，使用 Tailwind 套用樣式
- 你可以直接在 Cursor 中使用 shadcn/ui，不需要 v0

### 什麼是 shadcn/ui？
- 一個 React 設計系統
- 提供常見 UI 元件：按鈕、輸入框、卡片、核取方塊等
- 使用 Tailwind CSS 做樣式
- **特色**：元件會複製到你的專案中，可以自由修改
- 不是傳統的「匯入即用」函式庫

### 優勢
1. **完全可客製化** - 元件存在你的專案中
2. **一致性設計** - 所有元件風格統一
3. **不需要 v0** - 直接在任何 React 專案使用
4. **不需要 Next.js** - 任何 React 專案都可以用

## 操作步驟

### 步驟 1：建立 React 專案
```bash
npm create vite@latest ui -- --template react-ts
cd ui
```

### 步驟 2：安裝 Tailwind CSS
- Tailwind 使用 class names 來套用樣式
- shadcn/ui 依賴 Tailwind 來實現專業外觀

### 步驟 3：設定 TypeScript 配置
- 修改 `tsconfig.json`
- 修改 `tsconfig.app.json`

### 步驟 4：更新 Vite 配置
```bash
npm install -D @types/node
```

### 步驟 5：安裝 shadcn/ui
```bash
npx shadcn-ui@latest init
```
選項設定：
- Style: New York
- 其他選項依需求選擇

### 步驟 6：新增元件
```bash
npx shadcn add input button card
```
- 這會**複製**元件到你的專案中
- 不是引用第三方函式庫
- 可以自由修改這些元件

### 步驟 7：建立應用程式
範例：建立 Shopify clone
- 使用 Cursor AI 生成程式碼
- Prompt：「Implement a Shopify clone using shadcn components in React」
- 包含：產品列表、側邊欄、導覽列、搜尋功能

### 步驟 8：設定 Tailwind Config
確保 `tailwind.config.js` 正確設定，讓樣式能正確套用

### 步驟 9：客製化主題
1. 前往 shadcn/ui 網站
2. 複製主題 CSS
3. 貼到你的 CSS 檔案中
4. 可修改顏色變數

### 步驟 10：加入深色模式
- shadcn/ui 內建深色模式支援
- 只需幾個元件調整即可實現

## 重點功能

### shadcn/ui 特色
| 特色 | 說明 |
|------|------|
| 複製而非引用 | 元件複製到專案中，可自由修改 |
| Tailwind 驅動 | 使用 Tailwind CSS 做樣式 |
| 可客製化 | 建立元件變體、修改樣式 |
| 一致性 | 所有元件風格統一 |
| 深色模式 | 內建支援 |

### 常用元件
- Button（按鈕）
- Input（輸入框）
- Card（卡片）
- Checkbox（核取方塊）
- 更多...

## 學習筆記

### 💡 核心洞見
1. **v0 的魔法 = shadcn/ui** - 你可以繞過 v0 直接用
2. **Cursor + shadcn** - 比 v0 更靈活的組合
3. **元件可編輯** - 這是 shadcn 最大的優勢

### ⚡ 快速指令
```bash
# 初始化 shadcn
npx shadcn-ui@latest init

# 新增特定元件
npx shadcn add [component-name]

# 新增多個元件
npx shadcn add input button card
```

### 🔧 工作流程
```
Vite 建立專案 → 安裝 Tailwind → 安裝 shadcn →
新增元件 → Cursor AI 生成 UI → 客製化樣式
```

### 📌 vs v0 比較
| 方面 | v0 | Cursor + shadcn |
|------|-----|------------------|
| 靈活性 | 較低 | 較高 |
| 客製化 | 有限 | 完全自由 |
| 學習曲線 | 較低 | 較高 |
| 工具鏈 | Vercel 生態系 | 任何 React |

### 🔗 相關資源
- [shadcn/ui 官網](https://ui.shadcn.com)
- [Tailwind CSS](https://tailwindcss.com)
- [Vite](https://vitejs.dev)
- GitHub 範例專案（影片描述連結）
